lfrom launch import LaunchDescription
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription([
        Node(
            package='wall-eb',
            executable='output.py',
            name='tts_node',
            output='screen'
        ),

        Node(
            package='wall-eb',
            executable='content',
            name='content_node',
            output='screen'
        ),
    ])
